apiVersion: apps/v1
kind: Pod
metadata:
  name: nginx-pod
  namespace: ${OPENSHIFT_NAMESPACE}
spec:
  imagePullSecrets:
    - name: ghcr-pull-secret
  containers:
    - name: nginx
      image: ${IMAGE}
      ports:
        - containerPort: 8080
